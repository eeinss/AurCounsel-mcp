# Tools

This document describes the public purpose and behavioral contract of AurCounsel MCP tools. **Exact schemas must come from the live MCP server.** This file intentionally does not invent field names that have not been verified.

## Contract authority

For each tool, the authoritative order is:

1. live MCP tool discovery (`tools/list` or the equivalent exposed by the connected MCP client);
2. production request/response behavior;
3. this repository's documentation.

If this document differs from the machine-readable live contract, treat the difference as a documentation bug.

---

## `review_contract`

### Purpose

Submit a contract to AurCounsel for contract review.

### Expected lifecycle

```text
review_contract
-> job_id
-> get_job(job_id)
-> completed | failed
-> get_artifact(...) when completed
```

### Public guarantees

- The operation is asynchronous.
- A successful submission identifies a job.
- Completed deliverables are retrieved through the artifact interface.
- A failed job must not expose partial/generated internal artifacts as completed deliverables.

### Exact schema

**MACHINE TRUTH REQUIRED**

Executor must paste the exact live schema here, including:

- required fields;
- optional fields;
- accepted content/input representation;
- enum values;
- size or format constraints exposed by the public contract;
- exact response shape;
- error shape for invalid input.

### Verified example

**MACHINE TRUTH REQUIRED** — replace with a request that has been executed successfully against the public production endpoint.

---

## `draft_contract`

### Purpose

Submit a contract drafting request to AurCounsel.

### Public guarantees

- The operation is asynchronous unless the live schema explicitly states otherwise.
- A successful submission identifies a job.
- Final drafting deliverables are retrieved only after successful completion.

### Exact schema

**MACHINE TRUTH REQUIRED** — Executor must insert the exact live input and response schema.

### Verified example

**MACHINE TRUTH REQUIRED**.

---

## `compare_contracts`

### Purpose

Submit two contracts, versions, or other comparison inputs accepted by the live schema and ask AurCounsel to produce a comparison job.

### Public guarantees

- The comparison request produces a job identifier on successful submission.
- The job lifecycle is observed with `get_job`.
- Completed comparison deliverables are retrieved through `get_artifact`.

### Exact schema

**MACHINE TRUTH REQUIRED** — Executor must insert the exact live input and response schema and clarify the accepted representation of both comparison inputs.

### Verified example

**MACHINE TRUTH REQUIRED**.

---

## `get_job`

### Purpose

Retrieve the current public state of a previously submitted AurCounsel job.

### Public status vocabulary

```text
submitted
processing
completed
failed
unknown
```

### Terminal semantics

- `completed` — terminal success. Completed deliverables may be retrieved according to the artifact contract.
- `failed` — terminal failure. Do not retry artifact retrieval under the assumption that the job is merely unfinished.
- `unknown` — the requested job cannot currently be resolved as a known job. It is not a synonym for `processing`.

### Exact schema

**MACHINE TRUTH REQUIRED** — Executor must insert:

- exact input field(s);
- exact response shape;
- canonical status field;
- failure/error detail fields;
- whether artifact availability is embedded in the response;
- behavior for malformed versus unknown job identifiers.

### Verified examples

Executor should provide at least one live response for each status that is practical to capture, especially `completed`, `failed`, and `unknown`.

---

## `get_artifact`

### Purpose

Retrieve a completed deliverable generated by AurCounsel.

### Public guarantees

- Artifact retrieval is for deliverables exposed by the public artifact contract.
- Artifact availability must not be interpreted independently of terminal job state.
- A failed job's partial/generated internal output is not a completed deliverable.

### Exact schema

**MACHINE TRUTH REQUIRED** — Executor must insert:

- exact lookup fields;
- artifact identifier/type vocabulary;
- response shape;
- content type/encoding;
- metadata fields;
- not-found/unavailable behavior;
- any size or paging behavior exposed publicly.

### Verified example

**MACHINE TRUTH REQUIRED**.

---

## Schema synchronization checklist

Before a release of this repository, compare all five documented tools against one fresh live discovery capture. Verify:

- exact tool names;
- exact descriptions;
- required vs optional fields;
- JSON types;
- enums;
- defaults, if publicly declared;
- response shapes;
- public error responses;
- any newly added or removed tools.
